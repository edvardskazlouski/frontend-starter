export default ({ colors }) => ({
  container: {
    marginBottom: 20,
  },
  groupsSize: {
    color: colors.WHITE,
    backgroundColor: colors.LILAC_BUSH,
    marginBottom: 10
  },
  loader: {
    height:'100%'
  }
});
